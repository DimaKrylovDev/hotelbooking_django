# Система бронирования отелей

## 📋 Титульный слайд

**Название проекта:** Система бронирования отелей (Hotel Booking System)

**Разработчик:** [Ваше ФИО]

**Группа:** [Ваша группа]

---

## 🎯 Цель и задачи проекта

### Цель проекта
Разработка полнофункциональной веб-системы для бронирования номеров отелей с поддержкой ролевой модели доступа, модерации контента и управления бронированиями.

### Задачи проекта

1. **Реализация системы аутентификации и авторизации**
   - Кастомная модель пользователя с ролями (USER, STAFF, ADMIN)
   - Регистрация и вход в систему
   - Управление профилем пользователя

2. **Управление номерами**
   - Создание, редактирование и удаление номеров владельцами
   - Загрузка фотографий номеров (основное фото + галерея до 5 изображений)
   - Поиск номеров по городу, датам и количеству гостей
   - Фильтрация по типу номера, цене и удобствам

3. **Система бронирования**
   - Создание бронирований с проверкой доступности
   - Управление статусами бронирований (Pending, Confirmed, Cancelled)
   - История изменений бронирований
   - Автоматический расчет стоимости

4. **Система отзывов**
   - Создание отзывов гостями после бронирования
   - Модерация отзывов сотрудниками/администраторами
   - Ответы владельцев на отзывы
   - Рейтинговая система (1-5 звезд)

5. **Ролевая модель доступа**
   - Middleware для проверки прав доступа на основе URL-паттернов
   - Разделение функционала по ролям пользователей

---

## 🛠 Технологический стек

### Backend
- **Django 5.2.8** — веб-фреймворк
- **Python 3.13** — язык программирования
- **PostgreSQL** — система управления базами данных
- **psycopg2** — адаптер для работы с PostgreSQL

### Дополнительные библиотеки
- **Pillow** — обработка изображений
- **Django Forms** — валидация и обработка форм
- **Django Admin** — административная панель

### Frontend
- **HTML5/CSS3** — разметка и стилизация
- **Django Templates** — шаблонизатор

---

## 🚀 Краткая демонстрация работы приложения

### Основные функции

1. **Поиск номеров**
   - Пользователь вводит город, даты заезда/выезда и количество гостей
   - Система находит доступные номера с учетом существующих бронирований
   - Применение фильтров по типу, цене и удобствам

2. **Бронирование**
   - Выбор номера и создание бронирования
   - Автоматическая проверка конфликтов по датам
   - Расчет стоимости на основе количества ночей

3. **Управление номерами (для владельцев)**
   - Создание объявлений о номерах
   - Загрузка фотографий
   - Просмотр бронирований своих номеров

4. **Модерация отзывов (для staff/admin)**
   - Просмотр отзывов на модерации
   - Одобрение или отклонение отзывов
   - Добавление комментариев модератора

5. **Профиль пользователя**
   - Просмотр своих бронирований
   - История бронирований
   - Управление личными данными

---

## 🏗 Архитектура и ключевые модули

### Структура проекта

```
hotel_booking/
├── hotel_booking/          # Основные настройки проекта
│   ├── settings.py         # Конфигурация Django
│   ├── urls.py             # Главный URL-роутинг
│   └── wsgi.py             # WSGI конфигурация
├── main/                   # Основное приложение
│   ├── models.py           # Модели данных (User, Room, Booking, Review)
│   ├── views.py            # Представления (CBV и FBV)
│   ├── forms.py            # Формы для валидации данных
│   ├── urls.py             # URL-маршруты приложения
│   ├── middleware.py       # Middleware для проверки ролей
│   ├── admin.py            # Настройки админ-панели
│   └── templates/          # HTML-шаблоны
└── manage.py               # Утилита управления Django
```

### Модели данных (ER-диаграмма)

#### User (Пользователь)
- `id` — первичный ключ
- `first_name`, `last_name` — имя и фамилия
- `email` — электронная почта (уникальная)
- `phone` — телефон
- `password` — пароль (хэшированный)
- `role` — роль (USER, STAFF, ADMIN)
- `is_active` — активность аккаунта
- `created_at`, `updated_at` — временные метки

**Связи:**
- Один ко многим: `User` → `Room` (room_owner)
- Один ко многим: `User` → `Booking` (guest)
- Один ко многим: `User` → `Review` (guest, moderated_by)

#### Room (Номер)
- `id` — первичный ключ
- `room_owner` — владелец (ForeignKey → User)
- `room_type` — тип номера (Standard, Deluxe, Suite)
- `price_per_night` — цена за ночь
- `address` — адрес (город)
- `room_photo` — основное фото
- `capacity` — вместимость
- `size` — размер номера
- `amenities` — удобства (текстовое поле)
- `is_active` — активность объявления
- `created_at`, `updated_at` — временные метки

**Связи:**
- Один ко многим: `Room` → `RoomImage` (галерея)
- Один ко многим: `Room` → `Booking`
- Один к одному: `Room` → `Review`

#### Booking (Бронирование)
- `id` — первичный ключ
- `guest` — гость (ForeignKey → User)
- `room` — номер (ForeignKey → Room)
- `check_in_date` — дата заезда
- `check_out_date` — дата выезда
- `status` — статус (Pending, Confirmed, Cancelled)
- `total_cost` — общая стоимость
- `created_at`, `updated_at` — временные метки

**Ограничения:**
- `check_in_date < check_out_date` (CheckConstraint)

**Связи:**
- Один к одному: `Booking` → `BookingHistory`
- Один к одному: `Booking` → `Review`

#### Review (Отзыв)
- `id` — первичный ключ
- `booking` — бронирование (OneToOne → Booking)
- `guest` — гость (OneToOne → User)
- `room` — номер (OneToOne → Room)
- `rating` — оценка (1-5)
- `review_text` — текст отзыва
- `status` — статус модерации (Pending, Approved, Rejected)
- `moderated_by` — модератор (ForeignKey → User)
- `moderation_comment` — комментарий модератора
- `moderated_at` — дата модерации
- `owner_reply` — ответ владельца
- `owner_reply_at` — дата ответа
- `created_at`, `updated_at` — временные метки

#### RoomImage (Изображение номера)
- `id` — первичный ключ
- `room` — номер (ForeignKey → Room)
- `image` — изображение
- `created_at`, `updated_at` — временные метки

#### BookingHistory (История бронирования)
- `id` — первичный ключ
- `booking` — бронирование (OneToOne → Booking)
- `old_status` — предыдущий статус
- `new_status` — новый статус
- `changed_by` — кто изменил
- `change_description` — описание изменения
- `created_at`, `updated_at` — временные метки

### Ключевые модули

#### 1. Models (models.py)
- **BaseModel** — абстрактная модель с временными метками
- **UserManager** — кастомный менеджер пользователей
- **RoomManager** — менеджер с методом `get_available_rooms()` для поиска доступных номеров
- **Свойства моделей:**
  - `User.is_admin`, `User.is_staff`, `User.is_owner`
  - `Room.amenities_list` — парсинг удобств в список

#### 2. Views (views.py)
- **Class-Based Views (CBV):**
  - `HomeView`, `SearchResultsView` — поиск номеров
  - `ProfileView` — профиль пользователя
  - `RoomCreateView`, `RoomDeleteView` — управление номерами
  - `BookingCreateView`, `BookingDeleteView` — управление бронированиями
  - `ReviewCreateView`, `ReviewUpdateView`, `ReviewDeleteView` — управление отзывами
  - `ReviewModerationListView`, `ReviewModerateView` — модерация (staff/admin)

- **Function-Based Views (FBV):**
  - `signup_view`, `login_view`, `logout_view` — аутентификация
  - `edit_profile_view` — редактирование профиля
  - `update_room_view`, `update_booking_view` — обновление объектов
  - `confirm_booking_view` — подтверждение бронирования
  - `booking_history_view` — история бронирований

#### 3. Forms (forms.py)
- Валидация данных на уровне форм
- Проверка дат, паролей, доступности номеров
- Кастомные виджеты для улучшения UX

#### 4. Middleware (middleware.py)
- **RoleRequiredMiddleware** — централизованная проверка прав доступа
- Паттерны URL для разных уровней доступа:
  - `PUBLIC_URLS` — публичные страницы
  - `ADMIN_REQUIRED_URLS` — только для администраторов
  - `STAFF_REQUIRED_URLS` — для сотрудников и админов
  - `USER_ONLY_URLS` — только для обычных пользователей
  - `LOGIN_REQUIRED_URLS` — требуют авторизации

#### 5. Mixins (views.py)
- **AdminRequiredMixin** — проверка прав администратора
- **StaffRequiredMixin** — проверка прав сотрудника
- **UserRequiredMixin** — проверка, что пользователь не staff/admin

---

## 💻 Качество кода и документация

### Принципы организации кода

1. **Разделение ответственности**
   - Модели содержат бизнес-логику
   - Views обрабатывают HTTP-запросы
   - Forms валидируют данные
   - Middleware проверяет права доступа

2. **Использование Django Best Practices**
   - Class-Based Views для стандартных операций CRUD
   - Custom Managers для сложных запросов
   - Abstract Models для переиспользования кода
   - CheckConstraints для валидации на уровне БД

3. **Безопасность**
   - Хэширование паролей через Django
   - CSRF защита
   - Валидация данных на уровне форм и моделей
   - Проверка прав доступа через middleware и mixins

4. **Производительность**
   - Использование `select_related()` и `prefetch_related()` для оптимизации запросов
   - Компиляция регулярных выражений в middleware
   - Индексация в БД (через ForeignKey)

### Документация

- **README.md** — полное описание проекта
- **Комментарии в коде** — объяснение сложных участков
- **Docstrings** — описание классов и методов

---

## 📦 Инструкция по запуску

### Требования
- Python 3.13+
- PostgreSQL 12+
- pip

### Установка

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd hotel_booking
   ```

2. **Создание виртуального окружения**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

3. **Установка зависимостей**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройка базы данных**
   
   Создайте базу данных PostgreSQL:
   ```sql
   CREATE DATABASE hotel_booking;
   CREATE USER postgres WITH PASSWORD 'postgres';
   GRANT ALL PRIVILEGES ON DATABASE hotel_booking TO postgres;
   ```

   Или настройте переменные окружения:
   ```bash
   export POSTGRES_DB=hotel_booking
   export POSTGRES_USER=postgres
   export POSTGRES_PASSWORD=postgres
   export POSTGRES_HOST=localhost
   export POSTGRES_PORT=5432
   ```

5. **Применение миграций**
   ```bash
   python manage.py migrate
   ```

6. **Создание суперпользователя**
   ```bash
   python manage.py createsuperuser
   ```

7. **Запуск сервера разработки**
   ```bash
   python manage.py runserver
   ```

8. **Доступ к приложению**
   - Приложение: http://127.0.0.1:8000/
   - Админ-панель: http://127.0.0.1:8000/admin/

### Структура requirements.txt

```
Django==5.2.8
psycopg2-binary==2.9.11
Pillow==12.0.0
```

---

## 📸 Скриншоты

> **Примечание:** Добавьте скриншоты основных страниц приложения:
> - Главная страница с поиском
> - Результаты поиска номеров
> - Страница номера
> - Профиль пользователя
> - Форма бронирования
> - Страница модерации отзывов

---

## 🔑 Основные функции для демонстрации

### Для обычного пользователя:
1. Регистрация и вход
2. Поиск номеров по городу и датам
3. Просмотр деталей номера
4. Создание бронирования
5. Просмотр истории бронирований
6. Создание отзыва после бронирования

### Для владельца номера:
1. Создание объявления о номере
2. Загрузка фотографий
3. Просмотр бронирований своих номеров
4. Ответы на отзывы

### Для модератора/администратора:
1. Модерация отзывов
2. Одобрение/отклонение отзывов
3. Просмотр статистики

---

## 📊 Статистика проекта

- **Моделей данных:** 6 (User, Room, RoomImage, Booking, BookingHistory, Review)
- **Представлений:** 20+ (CBV и FBV)
- **Форм:** 9
- **URL-маршрутов:** 30
- **Шаблонов:** 20+
- **Middleware:** 1 (RoleRequiredMiddleware)
- **Роли пользователей:** 3 (USER, STAFF, ADMIN)

---

## 🎓 Выводы

Проект демонстрирует:
- Полное понимание архитектуры Django
- Работу с моделями данных и связями
- Реализацию системы аутентификации и авторизации
- Использование Class-Based и Function-Based Views
- Валидацию данных через формы
- Middleware для централизованной проверки прав
- Оптимизацию запросов к БД
- Следование best practices Django



